# zerra

# Backend Environment Variables
`APP_DOMAIN`: application's domain (e.g. app.zerra.hu)
`OPEN`: listen on 0.0.0.0
`DB_URL`: postgres database (e.g. postgresql://username:password@host:port/dtabase_name)
`REDIS_URL`: e.g. `redis://<user>:<pass>@localhost:6379/<db>`
